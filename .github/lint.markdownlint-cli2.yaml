config:
  default: true
  MD013: false   # line length — not enforced
  MD022: false   # blanks around headings — keep-a-changelog style
  MD024:
    siblings_only: true  # ### Added / ### Fixed repeated across versions
  MD032: false   # blanks around lists — keep-a-changelog style
